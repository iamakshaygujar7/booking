package com.train.booking.enums;

/**
 * Enum used for checking status of any record
 * A = Active
 * D = Deleted
 * I = Inactive
 * @author Akshay Gujar
 *
 */
public enum ActiveIndicator {
	A, D, I
}
